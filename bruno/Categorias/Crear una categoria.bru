meta {
  name: Crear una categoria
  type: http
  seq: 1
}

post {
  url: {{base_url}}/api/v1/categorias
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "nombre": "Categoria de prueba (Postman)",
    "tipo": "gasto",
    "color": "#EA580C",
    "icono": "prueba"
  }
}

tests {
  test("responde 201", function () {
      expect(res.getStatus()).to.equal(201);
  });
  bru.setVar("categoria_id", res.getBody().datos.id);
}

docs {
  Guarda el id en categoria_id.
}

