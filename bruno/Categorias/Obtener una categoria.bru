meta {
  name: Obtener una categoria
  type: http
  seq: 3
}

get {
  url: {{base_url}}/api/v1/categorias/{{categoria_id}}
  body: none
  auth: inherit
}

tests {
  test("responde 200", function () {
      expect(res.getStatus()).to.equal(200);
  });
}

docs {
  Por identificador.
}

