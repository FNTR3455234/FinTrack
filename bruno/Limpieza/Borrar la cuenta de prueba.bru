meta {
  name: Borrar la cuenta de prueba
  type: http
  seq: 2
}

delete {
  url: {{base_url}}/api/v1/cuentas/{{cuenta_id}}
  body: none
  auth: inherit
}

tests {
  test("responde 204", function () {
      expect(res.getStatus()).to.equal(204);
  });
}

docs {
  Ya no tiene movimientos: se puede borrar.
}

